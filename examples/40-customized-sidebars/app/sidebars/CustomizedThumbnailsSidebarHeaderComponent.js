import * as React from "react";

// Custom sidebar React component
export function CustomizedThumbnailsSidebarHeader() {
  return (
    <div style={{ color: "grey", textAlign: "center" }}>
      A simple custom sidebar header
    </div>
  );
}
