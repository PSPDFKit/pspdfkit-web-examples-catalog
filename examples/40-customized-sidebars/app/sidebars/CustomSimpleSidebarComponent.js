import * as React from "react";

// Custom sidebar React component
export function CustomSimpleSidebar() {
  return (
    <div style={{ color: "grey", textAlign: "center" }}>
      A simple custom sidebar
    </div>
  );
}
